package com.smarthome.devices.version2;

import com.smarthome.devices.Lock;

public class Version2Lock extends Lock {
    public Version2Lock(String id, String name) {
        super(id, name);
    }
}

